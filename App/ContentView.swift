import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("MdirX")
        }
        .frame(minWidth: 800, minHeight: 500)
    }
}

#Preview {
    ContentView()
}
